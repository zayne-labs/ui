---
title: Introduction
description: Composable, headless UI components and utilities built for flexibility and great developer experience
icon: BookOpenText
---

## Welcome to Zayne UI

Zayne UI is a headless React component library that provides unstyled composable building blocks. It manages all the complex logic, accessibility and state under the hood while leaving the visual design entirely up to you.

## Key features

import { Code2Icon, CpuIcon, LayersIcon, LeafIcon, PaintbrushIcon, PuzzleIcon } from "lucide-react";

<Cards>
	<Card title="Headless by default" icon={<CpuIcon />}>
		Zero styling opinions. Components handle functionality and accessibility while you own the design.
		Override with `className` or opt out of base styles entirely with the `unstyled` prop.
	</Card>
	<Card title="TypeScript-first" icon={<Code2Icon />}>
		Written in TypeScript with full inference. Generic render props narrow types automatically so no
		manual annotations are needed.
	</Card>
	<Card title="Tree-shakeable" icon={<LeafIcon />}>
		Each component lives at its own entry point. You can import only what you use to eliminate dead code
		automatically.
	</Card>
	<Card title="Compound components" icon={<PuzzleIcon />}>
		UI components use a compound pattern (`Component.Root`, `Component.Part`) for maximum composition
		flexibility without prop drilling.
	</Card>
	<Card title="Polymorphic & slottable" icon={<LayersIcon />}>
		Change the rendered element with the `as` prop, or delegate rendering entirely to a child element
		with `asChild`. Full type safety is preserved either way.
	</Card>
	<Card title="Styling freedom" icon={<PaintbrushIcon />}>
		Ships with an optional Tailwind CSS v4 preset for theme tokens and animations. Prefer vanilla CSS?
		Target components via `data-scope`, `data-part`, and `data-slot` attributes.
	</Card>
</Cards>

## What's included

The library provides two categories of components:

### UI Components

Interactive, stateful components that use the [compound component pattern](/docs/component-structure) for maximum flexibility:

- **[Card](/docs/ui/card)**: Composable card layouts with header, content, footer, and action sections. Every part is polymorphic and supports `asChild`.
- **[Carousel](/docs/ui/carousel)**: Slideshow with auto-slide, pause-on-hover, navigation controls, and customizable indicators.
- **[DragScroll](/docs/ui/drag-scroll)**: Adds mouse drag-to-scroll to any container. Supports horizontal, vertical, or both directions with configurable scroll amount and device constraints.
- **[DropZone](/docs/ui/drop-zone)**: Full-featured file upload with drag-and-drop, file validation, progress tracking, image previews, and upload lifecycle callbacks.
- **[Form](/docs/ui/form)**: Form system built on React Hook Form with composable field components, reactive watchers, state subscriptions, and automatic error accessibility.

### Utility Components

Lightweight rendering primitives that solve common React patterns declaratively:

- **[Show](/docs/utility/show)**: Conditional rendering with fallback support and type narrowing. Supports a multi-branch `control="content"` mode for complex conditions.
- **[For](/docs/utility/for)**: List rendering with empty-state fallbacks. Accepts arrays or numbers (for skeleton patterns). `ForWithWrapper` wraps output in a container element.
- **[Switch](/docs/utility/switch)**: Pattern matching for multi-way conditional rendering. Supports both value matching and boolean matching modes.
- **[Await](/docs/utility/await)**: Declarative async handling with built-in Suspense and ErrorBoundary. Manages pending, success, and error states via sub-components.
- **[Presence](/docs/utility/presence)**: Animate mount/unmount with CSS animations or transitions. Defers DOM removal until exit animations complete.
- **[ErrorBoundary](/docs/utility/error-boundary)**: Declarative error catching with reset via `errorResetKeys` or the `useErrorBoundary` hook.
- **[Teleport](/docs/utility/teleport)**: Portal that renders children into any DOM node by selector, ref, or element. Supports `insertPosition` for precise placement.
- **[ClientGate](/docs/utility/client-gate)**: Guards content behind client-side hydration. Safely access `window`, `localStorage`, etc. without SSR mismatches.
- **[Slot](/docs/utility/slot)**: The engine behind `asChild`. Merges parent props, event handlers, refs, and class names into a child element.
- **[SuspenseWithBoundary](/docs/utility/suspense-with-boundary)**: Combines `Suspense` and `ErrorBoundary` into a single wrapper for cleaner async component handling.

## Framework support

<Callout>React is currently available. Support for additional frameworks is planned.</Callout>

**Currently available:**

- React 19.x (with full TypeScript support)

**Coming soon:**

- Vue 3
- Svelte 5
- Solid.js

## Built on solid foundations

Built on top of [@zayne-labs/toolkit](https://github.com/zayne-labs/toolkit), a collection of utilities and React hooks that power the component internals.
