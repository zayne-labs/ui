---
title: DragScroll
description: Mouse drag-to-scroll functionality for horizontal or vertical scrollable containers.
---

## Installation

```package-install
@zayne-labs/ui-react
```

## Preview

<ComponentTabs name="drag-scroll-demo" />

## Basic Usage

```tsx
import { DragScroll } from "@zayne-labs/ui-react/ui/drag-scroll";

function Example() {
	return (
		<DragScroll.Root>
			<DragScroll.Prev>←</DragScroll.Prev>

			<DragScroll.List className="flex gap-4 overflow-x-auto">
				{items.map((item) => (
					<DragScroll.Item key={item.id}>{item.content}</DragScroll.Item>
				))}
			</DragScroll.List>

			<DragScroll.Next>→</DragScroll.Next>
		</DragScroll.Root>
	);
}
```

## Component Source

<ComponentSource
	name="drag-scroll"
	src="../../packages/ui-react/src/components/ui/drag-scroll/drag-scroll.tsx"
/>

## Component API

<Accordions>
	<Accordion title="DragScroll.Root">
		<auto-type-table path="../../../lib/ui-options.ts" name="DragScrollRootProps" />
	</Accordion>
	<Accordion title="DragScroll.List">
		<auto-type-table path="../../../lib/ui-options.ts" name="DragScrollListProps" />
	</Accordion>
	<Accordion title="DragScroll.Item">
		<auto-type-table path="../../../lib/ui-options.ts" name="DragScrollItemProps" />
	</Accordion>
	<Accordion title="DragScroll.Prev / Next">
		<auto-type-table path="../../../lib/ui-options.ts" name="DragScrollPrevProps" />
	</Accordion>
</Accordions>

## Hook Usage

For advanced use cases, use the `useDragScroll` hook directly:

```tsx
import { useDragScroll } from "@zayne-labs/ui-react/ui/drag-scroll";

function CustomScroller() {
	const { propGetters } = useDragScroll();

	return <div {...propGetters.getListProps()}>{/* items */}</div>;
}
```

## Data Attributes

| Component | Attribute       | Value               | Description                                    |
| :-------- | :-------------- | :------------------ | :--------------------------------------------- |
| `List`    | `data-dragging` | `"true" \| "false"` | Active when container is being dragged.        |
| `Prev`    | `data-disabled` | `"true" \| "false"` | Active when no more content to scroll back.    |
| `Next`    | `data-disabled` | `"true" \| "false"` | Active when no more content to scroll forward. |
| **All**   | `data-scope`    | `"drag-scroll"`     | Shared scope identifier.                       |
| **All**   | `data-slot`     | `drag-scroll-*`     | Combined identifier (e.g. `drag-scroll-item`). |
